# 分阶段开发与汇报计划

## 1. 使用方式

开发按阶段顺序推进。

## 2. 通用完成门槛

每个阶段建议满足以下条件：

- 阶段范围内的代码和文档已经完成；
- 新增行为有自动化测试，原有测试继续通过；
- 自动化测试不访问真实网络，不包含真实账号、密码、token 或未脱敏响应；
- 两人完成交叉检查，能够共同说明主要设计和已知限制；
- 需要人工验证的阶段有截图或脱敏记录；
- 已填写阶段报告，未完成项不得写成已完成；
- 项目负责人确认后再进入下一阶段。

默认回归命令：

```powershell
python -m pytest
```

## 3. 阶段清单

### 阶段 00：环境与现状基线

目标：两人的开发环境、项目副本和当前行为保持一致。

产物与验收：

- 两人分别完成虚拟环境创建和 `python -m pip install -e ".[dev]"`；
- 三种启动方式至少验证一种，界面可正常打开；
- 记录 Python 版本、依赖安装结果和基线测试数量；
- `python -m pytest` 全部通过；
- 确认交接副本不包含 `data/`、下载影像、凭据和虚拟环境。

汇报重点：环境版本、基线测试结果、启动截图、发现的环境问题。

### 阶段 01：冻结最小公共契约

目标：建立后续三个数据集共同依赖的最小类型和接口，不实现具体卫星逻辑。

产物与验收：

- 建立 `core/contracts.py` 和 `core/models.py`；
- 实现 `SearchRequest`、`ProductAsset`、`SatelliteProduct`；
- 实现 `AuthMode`、`AuthSpec`、`Credentials`；
- 实现 `DownloadPlan`、`DownloadResult`、`DownloadProgressEvent`；
- 定义 `DatasetPlugin` 和 `SatelliteProvider` 协议；
- 为字段校验、不可变性和默认值编写契约测试；
- 输出 `docs/provider-contract.md`。

汇报重点：接口字段表、关键取舍、契约测试结果、暂不支持的能力。

### 阶段 02：插件发现与 Fake Provider

目标：证明数据集可以通过插件注册和发现，不需要在主程序写卫星名称分支。

产物与验收：

- 实现 `core/registry.py`；
- 约定每个插件导出 `PLUGIN` 对象；
- 建立 S1、S2、Landsat Fake Provider；
- registry 能发现三个 Fake 插件并拒绝重复 `plugin_id`；
- 插件发现和错误处理测试全部通过。

汇报重点：插件发现流程演示、三个插件清单、异常场景测试。

### 阶段 03：通用搜索与多资产下载 Worker

目标：后台任务不再假设所有数据源都需要账号密码，或一个产品只有一个文件。

产物与验收：

- 实现通用搜索 worker；
- 实现可选认证和资产级顺序下载 worker；
- 支持产品级、资产级进度和取消；
- 单资产失败不删除其他已完成资产；
- 取消后保留 `.part` 文件；
- 使用 Fake Provider 覆盖匿名、账号密码、单资产、多资产和失败场景。

汇报重点：worker 状态流、取消演示、多资产失败隔离测试。

### 阶段 04：Sentinel-2 插件迁移

目标：将现有 Sentinel-2 功能迁入插件结构，业务行为保持不变。

产物与验收：

- 建立 `datasets/sentinel2/`；
- 使用固定矩形 AOI、日期、云量和最大结果数；
- 保留 CDSE 登录、下载、续传和校验行为；
- 旧导入路径有临时兼容层；
- 迁移前后的 Sentinel-2 回归用例全部通过。

汇报重点：迁移前后行为对照、兼容层说明、回归测试结果。

### 阶段 05：动态多数据源界面

目标：界面完全根据插件声明生成搜索控件、认证区域和结果列。

产物与验收：

- 数据集选择器列出三个 Fake 插件和 Sentinel-2 正式插件；
- `FieldSpec` 至少支持数字、枚举和布尔控件；
- `ColumnSpec` 驱动结果列；
- `AuthSpec` 驱动认证区域显示和隐藏；
- 切换数据集清空旧结果和下载选择；
- UI 中不存在按 `plugin_id` 或卫星名称硬编码的业务分支；
- UI 自动化测试不访问真实网络。

汇报重点：三种 Fake 插件切换演示、UI 测试、界面已知限制。

### 阶段 06：CDSE 公共客户端抽取

目标：Sentinel-1 和 Sentinel-2 共用认证、HTTP 和整包下载能力。

产物与验收：

- 建立 `shared/cdse.py`；
- 支持 OAuth2 登录和令牌刷新；
- 支持超时、429/5xx 重试和重定向；
- 支持 Range 续传、服务器忽略 Range、校验和原子改名；
- Sentinel-2 改用公共客户端后回归测试继续通过；
- HTTP 行为全部使用模拟响应测试。

汇报重点：公共能力清单、S2 迁移结果、续传和重试测试。

### 阶段 07：Sentinel-1 检索与解析

目标：完成 Sentinel-1 IW GRD 的离线检索构造和目录响应解析。

产物与验收：

- 建立 `datasets/sentinel1/`；
- 构造固定矩形 AOI 的 CDSE OData 查询；
- 支持 IW、GRD、极化和升降轨条件；
- 解析平台、模式、极化、轨道方向和相对轨道号；
- 分页结果去重并执行本地足迹相交；
- 使用脱敏 fixture 完成查询和解析测试。

汇报重点：请求参数、fixture 来源与脱敏说明、解析结果示例。

### 阶段 08：Sentinel-1 下载闭环

目标：Sentinel-1 从界面检索到整包下载形成完整用户流程。

产物与验收：

- Sentinel-1 插件接入 registry 和动态 UI；
- 完成认证、下载、取消、续传和校验；
- 自动化测试覆盖 401、429、5xx、200/206 和校验失败；
- 完成一次人工真实检索和小范围下载 smoke test；
- smoke test 记录不含凭据和签名 URL。

汇报重点：完整流程演示、下载结果、失败和续传验证。

### 阶段 09：Landsat 数据源选型

目标：先确定可实际使用的数据目录和资产访问方式，再编写 Provider。

产物与验收：

- 比较至少一个首选和一个备用 STAC 数据源；
- 验证国内网络可达性、认证、Collection ID、分页和速率限制；
- 验证 Landsat 8/9 Collection 2 Level-2 覆盖；
- 确认红、绿、蓝和 QA_PIXEL 的资产键、大小和校验字段；
- 记录 URL 是否过期以及重新获取方式；
- 输出 `docs/landsat-source-decision.md` 和脱敏 STAC fixture。

汇报重点：候选对比表、可复现验证命令、最终选择与风险。

### 阶段 10：Landsat 检索与解析

目标：将 Landsat STAC 项目转换成统一产品和固定四资产模型。

产物与验收：

- 建立 `datasets/landsat/`；
- 支持固定矩形 AOI、日期、云量和平台条件；
- 解析时间、平台、云量、path/row 和足迹；
- 外部资产键映射为 `red`、`green`、`blue`、`qa_pixel`；
- 缺失必要资产时给出明确错误；
- fixture 查询和解析测试全部通过。

汇报重点：STAC 请求、字段映射表、统一产品解析示例。

### 阶段 11：Landsat 多资产下载闭环

目标：完成 RGB + QA_PIXEL 顺序下载和界面接入。

产物与验收：

- Landsat 插件接入 registry 和动态 UI；
- 固定顺序下载 RGB + QA_PIXEL 到产品独立目录；
- 每个资产独立使用 `.part` 文件；
- 单资产失败保留其他已完成文件；
- 取消后可继续下载；
- 完成一次人工真实检索和资产下载 smoke test。

汇报重点：四资产目录、资产级进度、失败隔离和续传演示。

### 阶段 12：三数据源集成回归

目标：验证 S2、S1 和 Landsat 在同一应用中能够稳定切换和使用。

产物与验收：

- 三个正式插件均能被 registry 发现；
- 逐一完成检索、选择和下载流程；
- 数据集切换不残留结果、凭据状态或下载选择；
- 匿名 Landsat 不要求 CDSE 账号；
- 三个 Provider 通过同一套契约测试；
- 全量自动化测试通过，无真实网络依赖。

汇报重点：三数据源连续操作演示、全量测试结果、遗留缺陷清单。

### 阶段 13：最终验收与交付

目标：完成代码、文档、保密检查和可复现交付。

产物与验收：

- README、系统设计、Provider 契约和数据源决策文档与代码一致；
- 用户可见错误均有明确中文提示；
- 三个数据集各有一次脱敏 smoke test 记录；
- 交接包不包含 `data/`、下载影像、虚拟环境、缓存和凭据；
- 新环境按交接指南安装后，测试和应用启动均成功；
- 汇总已知限制和下一版本建议。

汇报重点：最终功能演示、验收清单、交接包校验结果、未完成范围。

## 4. 阶段报告模板

每个阶段复制以下模板到 `docs/progress/stage-XX-report.md`：

```markdown
# 阶段 XX 完成报告：阶段名称

## 基本信息

- 开始日期：
- 完成日期：
- 主实现者：
- 复核者：

## 本阶段目标

- 

## 已完成内容

- 

## 变更文件

- `path/to/file`：修改说明

## 验证结果

- 自动化测试命令：
- 通过数量：
- 人工演示步骤与结果：
- 截图或脱敏记录位置：

## 设计决定

- 决定：
- 原因：
- 影响：

## 问题与未完成项

- 无；或列出具体问题、影响和处理计划。

## 下一阶段建议

- 

## 双人确认

- 主实现者确认：
- 复核者确认：
```

报告必须记录真实测试结果。存在未完成项时应明确写出，不得通过降低验收标准将阶段标记
为完成。
